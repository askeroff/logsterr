"use strict";
exports.__esModule = true;
var Tasks = function (props) {
    return props.tasks.map(function (item) { return className = "dashboard__item-task"; }, key = { item: item, : ._id } >
        { item: item, : .taskName }, /strong>
        < span > { formatTime: function (item) { }, : .seconds });
};
/span>
    < /span>
    < /p>;
;
exports["default"] = Tasks;
